# Copy this file to .env and fill in your values.
# Do not commit .env – it is in .gitignore.

# PostgreSQL connection (Supabase, Neon, or local).
# For Supabase on Render: use the pooler (Transaction) URI – direct DB uses IPv6 and Render is IPv4-only.
#   Dashboard → Project Settings → Database → Connection string → "Transaction" (port 6543).
#   Example: postgresql://postgres.PROJECT_REF:PASSWORD@aws-0-REGION.pooler.supabase.com:6543/postgres
# If the password contains special characters (e.g. ], [, {, }), URL-encode them.
DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@YOUR_HOST:5432/postgres

# Secret used to protect POST /api/refresh (e.g. from cron-job.org).
REFRESH_SECRET=your-long-random-refresh-secret
